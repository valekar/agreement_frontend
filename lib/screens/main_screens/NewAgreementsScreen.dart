import 'package:flutter/material.dart';

class NewAgreementsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("New Agreements screen"),
    );
  }
}
